% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/litellm.R
\name{litellm_setup}
\alias{litellm_setup}
\title{Configure connection settings for a LiteLLM server}
\usage{
litellm_setup(api_key, base_url)
}
\arguments{
\item{api_key}{Character string. The API key used to authenticate
with the LiteLLM server. This key is usually provided by the
administrator of the server or the API provider.}

\item{base_url}{Character string. The base URL of the LiteLLM API,
including the \verb{/v1} path. For example:

\code{"https://my-litellm-server/v1"}

or for a local installation:

\code{"http://localhost:4000/v1"}}
}
\value{
This function returns no value. It stores the configuration
in the R session options.
}
\description{
This function sets up the connection to a LiteLLM server by storing
the API key and the base URL of the server in the current R session.
Other functions in this package (such as \code{litellm_chat()} and
\code{litellm_models()}) use these settings automatically when sending
requests to the server.
}
\details{
The settings are stored using \code{options()} and remain available
for the duration of the current R session.

This function typically needs to be called \strong{once per R session}
before using other functions in this package.

The API key is stored internally in:

\code{options("litellm_api_key")}

and the server URL in:

\code{options("litellm_base_url")}
}
\examples{
\dontrun{

litellm_setup(
  api_key = "MY_API_KEY",
  base_url = "https://my-server/v1"
)

}

}
